let disp = document.querySelector(".inptdisplay");

function sin(){
    disp.value = Math.sin(disp.value);
}
function cos(){
    disp.value = Math.cos(disp.value);
}

function tan(){
    disp.value = Math.tan(disp.value);
}

function sqrt(){
    disp.value = Math.sqrt(disp.value);
}